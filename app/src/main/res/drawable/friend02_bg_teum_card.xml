<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 배경색 (흰색) -->
    <solid android:color="@android:color/white" />

    <!-- 테두리 -->
    <stroke
        android:width="2dp"
        android:color="#E8ECFF" />

    <!-- 모서리 둥글게 -->
    <corners android:radius="10dp" />
</shape>
